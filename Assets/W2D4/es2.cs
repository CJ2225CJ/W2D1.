using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class es2 : MonoBehaviour
{
    [SerializeField]
    // Start is called before the first frame update
    void Start() //(Array)
    {
        
        {
            //int numeri = new int[4];
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
//crea un array di 4 numeri interi
//itera nela array e stampa in console la somma e il prodotto di tutti i numeri